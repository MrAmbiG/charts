apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsfwjs.fullname" . }}-test-connection"
  labels:
    {{- include "nsfwjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsfwjs.fullname" . }}:{{ .Values.service.port }}']
