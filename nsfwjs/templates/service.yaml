apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsfwjs.fullname" . }}
  labels:
    {{- include "nsfwjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nsfwjs.selectorLabels" . | nindent 4 }}
